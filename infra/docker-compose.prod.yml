# Production overlay: remove dev-only port exposures

services:
  postgres:
    ports: []

  rabbitmq:
    ports: []

  scraper:
    ports: []

  core-engine:
    ports: []

  grafana:
    ports: []
